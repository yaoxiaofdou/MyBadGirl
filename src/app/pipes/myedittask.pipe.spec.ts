import { MyedittaskPipe } from './myedittask.pipe';

describe('MyedittaskPipe', () => {
  it('create an instance', () => {
    const pipe = new MyedittaskPipe();
    expect(pipe).toBeTruthy();
  });
});
