import { MAlertDirective } from './m-alert.directive';

describe('MAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new MAlertDirective();
    expect(directive).toBeTruthy();
  });
});
